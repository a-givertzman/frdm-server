# Износ грузового каната

## 1. Описание алгоритма проверки долговечности грузового каната

Долговечность грузового каната зависит от множества факторов, но основным из них является **изгиб каната на блоках**. 

Некоторые комментарии:
- Чем больше диаметр блока, тем больше радиус изгиба каната и меньше повреждений каната. 
- Также стоит избегать схем запасовки каната, когда канат испытывает знакопеременный изгиб на блоках. 
- Необходимо учитывать, что долговечность каната зависит от того какое натяжение в канате, чем выше натяжение, тем быстрее канат выйдет из строя.

### Исходные данные:

**1. Геометрия системы:**

- Положение всех блоков (полиспаста) и барабана в пространстве (x, y, z координаты);
- Диаметры всех блоков и барабана (*$D_{block[i]}, D_{drum}$*).

**2. Характерные точки каната:**

Определяем координаты «характерных точек». 
- Характерными точками являются точки входа и схода каната с блоков и барабана
- между парами этих точек канат находится в изогнутом состоянии

**3. Длина каната:**

- Суммируется из:

    - Прямолинейных отрезков между блоками;
    - Изогнутые участки каната на каждом блоке;
    - Длину каната на барабане, на каждом слое укладки каната на барабане в зависимости от высоты подъёма

- Длина на каждом участке каната должна быть известна.

**4. Измеряемые параметры с датчиков:**

- Текущая длина выданного каната (в метрах), получаем из датчика *H(t)*;
- Угол наклона стрелы (*θ(t)*); (используется для определения координат блоков)
- Угол наклона хобота*
- Сила натяжения каната (*T(t)* — данные с тензодатчика).

**В данном контексте, всё зависит от конструкции. Если в устройстве (кране) две стрелы, то учитывается угол наклона каждой из них.
В кране с шарнирно-сочленённой стрелой (КСШ, knuckle boom) мы иногда используем внутреннюю терминологию: нижнюю стрелу называем «основной», а верхнюю — «хоботом». Также допустимы другие варианты обозначений, например, «основная и вспомогательная стрела» и т. д.*

## 2. Алгоритм расчета 

**1. Дискретизация каната:**

- Общая длина каната L
- Шаг дискретизации* Δx (например, 0.01 м)

**Шаг дискретизации будет зависеть от модели, её размеров, то есть от диаметров блоков и общей длины каната и например режима работы. (Например, компенсация качки с амплитудой 0,5м или 10 м, в первом случае нам нужна более мелкая дискретизация)* 

- Количество точек: `N = L / Δx` 

То есть получаем массив точек по длине каната от `0` до `L` c шагом `Δx`:

```math
points = [x0, x1, x2, ..., xN]
```

**2. Определение массива участков изгиба(характерных точек)**

```math
bending\_zones = [(a1, b1, D1), (a2, b2, D2), ...]
```

- где **a1 и b1** - координаты начала и конца зоны изгиба (вдоль каната),
- **D1** - диаметр блока в этой зоне.

⚠️*Все координаты (xi, a1, b1) считаются вдоль каната от точки крепления на барабане — в метрах.*

**3. Фильтрация активных точек**

Для каждой точки каната проверяем, участвует ли она в работе — то есть вышла ли она из барабана. Это определяется сравнением координаты точки с текущей высотой подъёма $H(t)$ получаемой с датчика. Если координата точки меньше или равна $H(t)$,  значит точка выдана и находится в работе.

```math 
xi <= H(t)
```  

- $x_{i} <= H(t)$ — точка реально участвует в работе (она не на барабане).
участвует в анализе
- $x_{i} > H(t)$ — тогда точка находится на барабане и исключается из анализа.  

--> В последующей итерации расчета, надо добавить какое-то количество витков на барабане, так как канат в этом месте испытывает натяжение и изгиб имеется

**4. Проверка на bending zone**

Далее, для активных точек проверяем, находятся ли они в зоне изгиба — то есть между двумя характерными точками, которые определяют вход и выход каната на конкретном блоке. Эти участки задаются заранее, исходя из геометрии полиспаста и размеров блоков.

```math 
a <= xi <= b
```

Это значит, что точка физически находится в зоне изгиба на блоке (между входом и выходом).

**5. Прибавляем износ за перегиб:**
```math 
bends[i] += T / D
```
Если точка находится в таком участке, значит она претерпевает изгиб на этом блоке, и ей добавляется "износ" или счётчик перегибов. Величина этого износа пропорциональна текущему натяжению каната и обратно пропорциональна диаметру блока.

Таким образом, проходя по всем точкам, мы суммируем количество и степень перегибов для каждой точки, учитывая её положение на канате, текущую высоту подъёма и параметры блоков.


### Что делаем с этими данными?

- В массиве *bends[i]* мы накапливаем износ для каждой точки каната с течением времени.

- После каждой итерации пересчитываем этот массив.

- Сравниваем вычисленный износ с ресурсом

- Визуализировать график износа: x = points, y = bends.


## 3. Учет нагрузки 
Так как в алгоритме расчёта фигурирует натяжение каната T, важно уточнить, что оно не является постоянным во времени.
Натяжение зависит от множества факторов — таких как нагрузка, режим работы, движение груза, а также внешние воздействия (например, качка).

**Причины изменения натяжения каната:**

**1. Изменение веса груза**
- Когда поднимается или опускается груз, натяжение изменяется.
- Если груз раскачивается, возникают колебания нагрузки.

**2. Изменение угла наклона стрелы**
- При изменении геометрии полиспаста меняется распределение сил.
- Особенно важно для кранов и подъемных машин с переменной стрелой.

**3. Динамика работы**

- Разгон, торможение, ветер, качка → всё это создаёт переменную нагрузку.

Таким образом, все эти влияния уже заложены в значение натяжения каната T(t), получаемое с тензодатчика. Это значит, что в алгоритме износ рассчитывается с учётом текущей нагрузки и условий работы — через актуальное значение T.

## 4. Пример 
 Исходные данные:
- Канат длиной 20 м, шаг анализа — 0.01 м
→ точки: x = 0, 0.01, 0.02, ..., 20 м
- Зона изгиба: от 5 до 7 м (включительно), диаметр блока D = 0.5 м
- Данные с тензодатчика и датчика длины (временные шаги):

| Время (сек) | Натяжение T (Н) | H (м) — длина выданного каната |
| ----------- | --------------- | ------------------------------ |
| 0           | 10000           | 12                             |
| 1           | 8000            | 14                             |
| 2           | 12000           | 10                             |

**Шаг 1: t = 0**
-
- Общая длина каната L = 20 м
- Шаг дискретизации Δx = 1 м
- T = 10000 Н
- H = 12 → участвуют точки от 0 до 12м

**1. Дискретизация каната**
- Количество точек: N = L / Δx = 20 / 1 = 20 точек
- То есть получаем массив точек по длине от 0 до L c шагом Δx:

```math 
points = [x0, x1, x2, ..., xN] = [1, 2, ..., 20]
``` 

**2. Определение характерных точек**
- Характерные точки, исходя из зоны изгиба 
  
```math 
bending_{zones} = (a1, b1, D1) → (5, 7, 0.5)
```

**3. Фильтр активных точек**
- Проверяем входят ли наших точки в выданный канат, точка реально участвует в работе
```math 
xi <= H(t)
```

- То есть проходят точки, которые меньше 12 м

**4. Проверка на bending zone**

Проверяемя попадают ли точки на зону изгиба (характерные точки)

```math 
a <= xi <= b \\
5 <= xi <= 7
```

**5.  Накапливаем износ**

- Если точка находится в таком участке, значит она претерпевает изгиб на этом блоке, и ей добавляется "износ" счётчик перегибов.

```math 
bends[i] += T / D \\
bends[i] = T/D = 10000/0.5 = 20000
```

→ накопим:

$$
x_{5} : +20000  \\
x_{6} : +20000  \\
x_{7} : +20000
$$

**Шаг 2: t = 1**
-
- T = 8000 Н
- H = 14 → участвуют точки от 0 до 14
- x = 5, 6, 7 — снова в зоне изгиба и активны

$$bends[i] = T/D = 8000/0.5 = 16000$$


→ накопим:

$$
x_{5} : +16000  \\
x_{6} : +16000  \\
x_{7} : +16000
$$

**Шаг 3: t = 2**
-
- T = 12000 Н
- H = 10 → участвуют точки [x0, ..., x10]
- x = 5, 6, 7 — снова активны

$$bends[i] = T/D = 12000/0.5=24000$$

→ накопим:  
$$
x_{5} : +24000  \\
x_{6} : +24000  \\
x_{7} : +24000
$$

Итоговый результат по участкам:
-
| x (м)             | Итоговый износ                    |
| -----             | --------------------------------- |
| **$x_{0}-x_{4}$** | 0                                 |
| **$x_{5}$**       | 20000 + 16000 + 24000 = **60000** |
| **$x_{6}$**       | 60000                             |
| **$x_{7}$**       | 60000                             |
| **$x_{8}-x_{20}$**| 0                                 |

- Для определения износа используется количество раз которое участок каната переходил из прямолинейного состояния в изогнутое(то есть если точка )
- Износ копится только на участках, где есть изгиб И где канат активен (выдан с барабана).
- Он зависит от силы натяжения на каждом шаге, т.е. от времени.


Чтобы понимать, когда канат подлежит замене, нужно сравнивать накопленный износ с допустимым (предельным) ресурсом каната. Для это рассмотрим доказательство усталостной прочности, которая будет приведена в разделе ниже

## 5. Сравнение знаения перегибов с предельным значением

Во время перегиба мы расчитываем не только величину "износа" (T/D), но и количество перегибов, которую обозначим $w_{tot}$. Далее необходимо, полученное число перегибов привести к относительному числу перегибов по формуле: 

```math
v_r = \frac{w_{tot}}{w_D} 
```

где:

- $w_{tot}$ — общее количество перегибов за срок службы каната  
- $w_D$ — количество перегибов в точке отсчёта: $w_D = 5 \cdot 10^5$


### 5.1 Предельная расчётная сила в канате

#### 5.1.1 Формула

**!!!Упростить и учитывать только $F_u$ и $s_r$ ????**

```math
F_{Rd,f} = \frac{F_u}{\sqrt[3]{s_r} \cdot \gamma_{rf}} \cdot f_f
```

**Пояснения:**

- $F_u$ — минимальное разрывное усилие каната;  
- $s_r$ — параметр истории усилия;  
- $\gamma_{rf}$ — коэффициент запаса при усталости;  
- $f_f$ — поправка на внешние факторы.


#### 5.1.2 Параметр истории усилия

$$
s_r = k_r \cdot \nu_r
$$

- $k_r$ — спектральный коэффициент усилия;  
- $\nu_r$ — относительное число перегибов:  
 

#### 5.1.3 Расчёт $k_r$

`Можем ли мы упростить и не учитывать этот коэффициент ?k_r = 1?`

```math 
k_r = \sum_{i=1}^{i_{\text{max}}} \left( \frac{F_{Sd,i}}{F_{Sd}} \right)^3 \cdot \frac{w_i}{w_{tot}}
```

- $F_{Sd,i}$ — усилие в $i$-м цикле;  
- $F_{Sd}$ — максимальное из всех $F_{Sd,i}$;  
- $w_i$ — перегибы в одном перемещении;  
- $w_{tot}$ — общее количество перегибов за срок службы;  
- $i_{\text{max}} = \frac{C}{l_r}$ — общее число перемещений на канат;  
- $C$ — число циклов работы крана;  
- $l_r$ — количество канатов за срок службы.


### 5.2 Расчётная сила в канате

#### 5.2.1 Основная формула

Для вертикального подъёма расчётная сила определяется как:

```math
F_{Sd,f} = \frac{m_{Hr} \cdot g}{n_f} \cdot \phi^* \cdot f^*_{S2} \cdot f^*_{S3}
```

**Где:**

- $m_{Hr}$ — масса, приходящаяся на рассматриваемую ветвь каната;  
- $g$ — ускорение свободного падения;  
- $n_f$ — число ветвей, несущих груз;  
- $\phi^*$ — динамический коэффициент;  
- $f^*_{S2}, f^*_{S3}$ — коэффициенты, учитывающие угол и горизонтальные силы.


`можем ли мы учитывать только эту часть ?`
```math
F_{Sd,f} = \frac{m_{Hr} \cdot g}{n_f} 
```


---

#### 6.2.2 Инерционные эффекты

**Если один изгиб (w = 1):**

```math
\phi^* = \phi
```

**Если w ≥ 2:**

```math
\phi^* = \sqrt[3]{\frac{(w - 1) + \phi^3}{w}}
```

- $w$ — число изгибов за цикл (см. Приложение A);  
- $\phi$ — динамический коэффициент (см. 5.2.2 из стандарта).

---

> Примечания:
> - Используются коэффициенты надёжности: $\gamma_p = 1$, $\gamma_n = 1$.
> - КПД канатной системы принимается равным 1.







## 6. Доказательство усталостной прочности

*Данный материал взят из DD CEN/TS 13001-3-2:2004 — это документ, разработанный Европейским комитетом по стандартизации (CEN), относящийся к стандартам безопасности грузоподъемного оборудования.*

### 6.1 Общие положения

Согласно результатам испытаний
1. Чем сильнее тянут канат, тем быстрее он изнашивается.
   - Если нагрузку увеличить в 2 раза, канат износится в 4 раза быстрее (потому что зависимость квадратная).
2. Чем больше изгибов, тем толще должен быть барабан или шкив.
   - Чтобы канат служил дольше, нужно увеличивать диаметр барабана (D) относительно диаметра каната (d).
   - Формула связывает эти параметры с числом изгибов $w_{tot}$.


```math
\frac{D}{d} \sim 1{.}125^{\log_2(w_{tot})} 
```

При проектировании грузоподъёмных систем необходимо учитывать два ключевых фактора, влияющих на усталостную долговечность каната:

**1. Зависимость от нагрузки:**
Усталостная прочность каната имеет кубическую зависимость от приложенной нагрузки. Это означает, что увеличение рабочего натяжения в 2 раза приводит к 8-кратному (2³) сокращению ресурса каната.

**2. Влияние изгибных циклов:**
Для обеспечения требуемого ресурса необходимо соблюдать соотношение между:

   - диаметром барабана/блока (D)
   - диаметром каната (d)
   - количеством изгибных циклов (w_tot)


Экспериментально установлено, что при удвоении количества изгибных циклов минимально допустимое отношение D/d должно увеличиваться в 1.125 раза (коэффициент 2^0.125).

Методика подсчёта циклов:

- Полный рабочий цикл (подъём + опускание) учитывается как один изгибный цикл
- В системах с реверсивным движением (например, тележки) каждый проход в одном направлении считается отдельно

Для подтверждения усталостной прочности должно быть доказано, что:

```math
F_{Sdf} \leq F_{Rdf} 
```

где:

- $F_{Sdf}$ — расчётная сила в канате на усталость  
- $F_{Rdf}$ — предельная расчётная сила в канате на усталость


### 6.2 Расчётная сила в канате

#### 6.2.1 Основные условия

Расчётная сила в канате $F_{Sdf}$ должна определяться только для регулярных нагрузок (сочетания нагрузок типа A)*, с применением коэффициентов надёжности:

- коэффициент надёжности по нагрузке $γ_{p}$ = 1
- коэффициент риска $γ_{n}$ = 1
- КПД каната также принимается равным 1.

![alt text](image-8.png)

Для вертикального подъема:

```math
​F_{Sd,f} = \frac {m_{Hr} \cdot g}{n_f} \cdot \phi^*\cdot f_{S2}^* \cdot f_{S3}^*
```

где:  
$m_{Hr}$ — масса груза поднимаемого канатом $m_{H}$,  (или та часть массы, которая действует на рассматриваемую ветвь каната, см. рисунок 1 ниже);  
$g$ — ускорение свободного падения;   
$n_f$ — количество ветвей (разветвлений) каната, несущих груз $m_{Hr}$;  
$\phi^*$ — динамический коэффициент, учитывающий инерционные и гравитационные воздействия (см. 5.2.2 Инерционные эффекты);  
$f_{S2}$, $f_{S3}$ — коэффициенты увеличения усилия в канате (см. 6.2.3 и 6.2.4в стандарте).

<center>
  <img src="image.png" width="300"/>
</center>

#### 6.2.2 Инерционные эффекты
Поскольку инерционные эффекты действуют кратковременно, они не влияют на все изгибы. Поэтому динамические коэффициенты $\phi^*$ можно рассчитать по формуле:

**Если число изгибов w=1:**

```math
\phi^* = \phi
```

**Если число изгибов w ≥ 2:**
```math
\phi^* = \sqrt[3]{\frac{(w - 1) + \phi^3}{w}}
```

где w  - соответствующее число изгибов на одно подъемное движение (см. приложение А).  
$\phi$ -  is the dynamic factor (see 5.2.2 or 5.3.3)


#### 6.2.3 Непараллельные ветви каната

Для проверки усталостной прочности учитывается распределение высоты и угла в рабочем диапазоне:

```math
f^*_{S2} = \sqrt[3]{\int_{z_{\text{min}}}^{z_{\text{max}}} \frac{q(z)}{\cos^3 \beta(z)} \, dz}
```

**Где:**

- $z$ — координаты по высоте (см. рисунок 7);  
  - $z_{\text{ref}}$ — эталонная (опорная) высота;  
  - Рабочий диапазон: от $z_{\text{min}}$ до $z_{\text{max}}$;  
  - Наиболее часто используемый диапазон: от $z_1$ до $z_2$;  
- $\beta(z)$ — угол между канатом и линией действия силы;  
- $q(z)$ — плотность распределения использования крана по высоте.

Нормировка плотности:

```math
\int_{z_{\text{min}}}^{z_{\text{max}}} q(z) \, dz = 1```
```

Если кран работает примерно равномерно по всем высотам в диапазоне $z_1$ – $z_2$, функция плотности считается постоянной:

```math
q(z) = \frac{1}{z_2 - z_1}
```

И коэффициент $f^*_{S2}$ может быть рассчитан по упрощённой формуле:

```math
f^*_{S2} = 1 + \left[ \frac{1}{\cos \beta(z_2)} - 1 \right] \cdot \left( \frac{z_{\text{ref}} - z_2}{z_{\text{ref}} - z_1} \right)^{0.9}
```

<center>
    <img src="image-1.png" width="300"/>
</center>

#### 6.2.4 Горизонтальные силы при вертикальном подъёме

Если горизонтальное ускорение и ускорение подъёма действуют одновременно, коэффициент $f^*_{S3}$ рассчитывается как:

```math
f^*_{S3} = f_{S3}
```

**Где:**

- $f_{S3}$ — коэффициент увеличения усилия в канате, рассчитанный по среднему углу $\gamma$ (см. 5.2.5).

Если горизонтальные силы и ускорение подъёма **не действуют одновременно регулярно**, можно принять:

```math
f^*_{S3} = 1
```

---

### 6.3 Предельная расчётная сила в канате

#### 6.3.1 Основная формула

Предельная расчётная сила $F_{Rd,f}$ определяется по формуле:

```math
F_{Rd,f} = \frac{F_u}{\sqrt[3]{s_r} \cdot \gamma_{rf}} \cdot f_f
```

**Где:**

- $F_u$ — минимальное разрывное усилие каната, заданное производителем;
- $s_r$ — параметр истории усилия в канате;
- $\gamma_{rf}$ — минимальный коэффициент запаса прочности каната при усталости;
- $f_f$ — коэффициент дополнительных влияний.

---

#### 6.3.2 Параметр истории усилия в канате

По аналогии с параметром истории напряжения (см. EN 13001-1), параметр $s_r$ определяется как:

```math
s_r = k_r \cdot \nu_r
````

**Где:**

- $k_r$ — спектральный коэффициент усилия;
- $\nu_r$ — относительное общее число изгибов.

#### 5.3.3 Коэффициент спектра усилий в канате

Коэффициент спектра усилий в канате $k_r$ рассчитывается по формуле:

```math
k_r = \sum_{i=1}^{i_{max}} \left( \frac{F_{Sd,i}}{F_{Sd}} \right)^3 \cdot \frac{w_i}{w_{tot}} 
```

где:

- $i$ — индекс одного подъёмного движения с $F_{Sd,i}$  
- $i_{max}$ — общее количество подъёмных движений на канат, учитывающее все рабочие циклы, количество которых на канат равно $C / l_r$  
- $F_{Sd,i}$ — расчётное усилие в канате при подъёме $i$  
- $F_{Sd}$ — максимальное расчётное усилие в канате  
- $w_i$ — соответствующее количество перегибов за одно подъёмное движение $i$ (см. Приложение A)  
- $w_{tot}$ — общее количество перегибов за срок службы каната  
- $C$ — общее количество рабочих циклов за срок службы крана (см. EN 13001-1)  
- $l_r$ — количество канатов, используемых в течение срока службы крана (см. Приложение B)

---

Общее количество перегибов $w_{tot}$ рассчитывается по формуле:

```math
w_{tot} = \sum_{i=1}^{i_{max}} w_i 
```

где:

- $w_i$ — количество перегибов на одно подъёмное движение $i$  
- $i_{max}$ — общее количество подъёмных движений на канат

---

#### 6.3.4 Относительное количество перегибов

Относительное количество перегибов рассчитывается по формуле:

```math
v_r = \frac{w_{tot}}{w_D} 
```

где:

- $w_{tot}$ — общее количество перегибов за срок службы каната  
- $w_D$ — количество перегибов в точке отсчёта: $w_D = 5 \cdot 10^5$

---

#### 6.3.5 Минимальный коэффициент прочности каната

Минимальный коэффициент прочности каната $\gamma_r$ должен быть:

```math
\gamma_r = 7 
```

---

### 6.4 Дополнительные влияния на предельное расчётное усилие в канате

#### 6.4.1 Основная формула

Фактор $f_r$ учитывает дополнительные влияния на предельное расчётное усилие в канате:

```math
f_f = f_{f1} \cdot f_{f2} \cdot f_{f3} \cdot f_{f4} \cdot f_{f5} \cdot f_{f6} \cdot f_{f7} 
```

где:

- $f_{f1}$ до $f_{f7}$ — коэффициенты влияния согласно пунктам 5.4.2 до 5.4.7

---

#### 6.4.2 Диаметры барабана и блоков

Дополнительное требование: отношение $D/d$ (диаметр перегибающего элемента к диаметру каната) должно увеличиваться с числом перегибов $w_{tot}$:

```math
D = \min(D_{sheave};\ 1,125 \cdot D_{drum};\ 1,125 \cdot D_{comp})
```

Референсное значение отношения $D/d$:

```math
R_{Dd} = 10 \cdot 1.125^{log_{2}\cdot (\frac{s_r}{0.004})}
```

**Table 5 — Reference ratio $R_{Dd}$**

| Class     | $S_{R0}$ | $S_{R1}$ | $S_{R2}$ | $S_{R3}$ | $S_{R4}$ | $S_{R5}$ | $S_{R6}$ | $S_{R7}$ | $S_{R8}$ | $S_{R9}$ |
|-----------|----------|----------|----------|----------|----------|----------|----------|----------|----------|----------|
| $R_{Dd}$  | 11,2     | 12,5     | 14,0     | 16,0     | 18,0     | 20,0     | 22,4     | 25,0     | 28,0     | 31,5     |


Коэффициент $f_{f1}$ рассчитывается:

```math
f_{f1} = \frac{D/d}{R_{Dd}}
```

Значение $D/d$ должно быть не менее 11.2, и $f_{f1}$ должен быть больше 0.75


#### 6.4.3 Прочность проволоки

Нелинейная зависимость между пределом прочности на разрыв Rr проволоки и предельным расчетным усилием каната должна быть учтена:

Если $R_r > 1770$:

```math
f_{f2} = (\frac{1770}{R_r})^{0.4}
```

Если $R_r \leq 1770$:

```math
f_{f2} = 1
```

где:

- $R_r$ — уровень прочности на разрыв (например, 1770, 1960 и т.д., согласно EN 12385-4)

#### 6.4.4 Угол отклонения (флит-угол)

Флит-углы на блоках или барабанах должны учитываться положительно. Для выбранной точки каната расчётный флит-угол $\delta$ определяется по формуле:

```math
\delta = \sqrt[3]{\frac{\sum_{j=1}^{n} \delta_j^3}{n}} 
```

где:

- $\delta_j$ — угол отклонения в точке касания $j$ каната с барабаном или шкивом  
- $n$ — количество точек касания, через которые проходит наиболее изгибаемая часть каната (например, $n = 6$)

![alt text](image-2.png)

**Таблица 6 — Коэффициент $f_{f3}$ (влияние угла схода)**

| Проектный угол схода $\delta$ | Нескручивающийся канат | Скручивающийся канат |
|-------------------------------|-------------------------|-----------------------|
| ≤ 0,5°                        | 1,0                     | 1,0                   |
| 1,0°                          | 0,9                     | 0,9                   |
| 2,0°                          | 0,75                    | 0,7                   |
| 3,0°                          | 0,7                     | —                     |
| 4,0°                          | 0,67                    | —                     |

*Промежуточные значения могут быть интерполированы.*


#### 6.4.5 Смазка каната

Для смазанных канатов коэффициент $f_{f4}$ принимается равным:

```math
f_{f4} = 1
```

Для канатов без смазки (например, в чистых помещениях):

```math
f_{f4} = 0{,}5
```

#### 6.4.6 Многослойная намотка барабана

Многослойная укладка каната снижает предельную расчётную силу каната.  
Следует применять коэффициент:

```math
f_{f5} < 0{,}8
```


#### 6.4.7 Радиус желоба

Соотношение радиуса желоба $r_g$ к диаметру каната $d$, а также угол $\omega$ между боковыми стенками шкива (см. рисунок 9) учитываются с помощью коэффициента $f_{f6}$ по таблице ниже.

<center>
    <img src="image-3.png" width="200"/>
</center>


**Таблица 7 — Коэффициент $f_{f6}$**
<center>

| $r_g/d$ | $\omega$            | $f_{f6}$ |
|---------|----------------------|----------|
| 0,53    | ≤ 60°                | 1,00     |
| 0,55    | ≤ 60°                | 0,84     |
| 0,6     | ≤ 60°                | 0,75     |
| 0,7     | ≤ 60°                | 0,63     |
| 0,8     | ≤ 60°                | 0,58     |
| ≥ 1,0   | *Нет требований*     | 0,54     |

</center>


*Промежуточные значения могут быть интерполированы.*

---

#### 6.4.8 Типы канатов

Различия в усталостной прочности при изгибе различных типов канатов учитываются коэффициентом $f_{f7}$, определяемым по формуле:

```math
f_{f7} = \frac{1}{t}
````

где:

- $t$ — коэффициент типа каната.

Для нескручивающихся канатов с 6–10 наружными прядями в большинстве случаев применяется $t = 1$. Для других типов канатов значения $t$ могут варьироваться от **0,95 до 1,25** и указываются производителем каната.

### <div align="center">Приложение А
### <div align="center">Количество значимых изгибов</div>


Один подъемный цикл включает операции подъема и опускания груза. Количество значимых изгибов **$w$** каната за один цикл определяется для наиболее нагруженного участка путем суммирования значений изгибов **$w_c$** в соответствии с таблицами А.1 и А.2.  

Если нагрузки при подъеме и опускании различаются (например, при размещении груза на верхнем уровне), следует использовать **$w/2$** для поднятого груза и **$w/2$** для опущенного. В таких случаях как подъем, так и опускание учитываются как отдельные подъемные циклы при расчете коэффициента спектра нагрузок каната.  

![alt text](image-4.png)

**В таблице А.2** приведены примеры расчетов для случаев, когда наиболее нагруженный участок каната проходит от барабана через все блоки.

![alt text](image-5.png)

Если в течение цикла канат проходит только через часть блоков, значение $w$ зависит от длины хода. На рис. А.1 представлен соответствующий пример

![alt text](image-6.png)
